const os = require("os");

// Platform
console.log(os.platform());

// CPU arch
console.log(os.arch());

// CPU core info
console.log(os.cpus());

// Total memory
console.log(os.totalmem());

// Home dir
console.log(os.homedir());
